# Secrets directory

This directory is for runtime certificates and signing keys used by SIFEN.

Expected files (not tracked by git):
- `F1T_65478.p12`
- `cert.pem`
- `key.pem`

Recommended permissions:
- files: `chmod 600`
- directory: `chmod 700`
