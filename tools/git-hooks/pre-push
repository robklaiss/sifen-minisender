#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

PYTHON_BIN="/Users/robinklaiss/Dev/sifen-minisender/.venv/bin/python"
if [[ ! -x "$PYTHON_BIN" ]]; then
  echo "[pre-push] No se encontró ejecutable: $PYTHON_BIN" >&2
  exit 1
fi

"$PYTHON_BIN" -m pytest -q tests/test_guardrail_*.py
